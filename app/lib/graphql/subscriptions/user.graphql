subscription GetUserStream($_id: uuid!) {
  user(where: { _id: { _eq: $_id } }) {
    _id
    _cr
    _up
    auth0_id
    email
    name
    app_metadata
    beta_access
    content_admin
    email_verified
    preferred_name
    picture
    nickname
  }
}
