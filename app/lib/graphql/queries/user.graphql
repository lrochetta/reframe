query FetchUser($auth0_id: String!) {
  user(where: { auth0_id: { _eq: $auth0_id } }) {
    _id
    _cr
    _up
    email
    name
    auth0_id
    nickname
    picture
    email_verified
    app_metadata
    user_metadata
    beta_access
    content_admin
  }
}
