mutation InsertInviteList($objects: [invite_insert_input!]!) {
  insert_invite(objects: $objects) {
    affected_rows
    returning {
      _id
      _cr
      _up
      email
      num_reminders
      accepted
      last_reminder
      workspace_id
      inviter
      promo_name
    }
  }
}
